server:
  port: 8080

spring:
  application:
    name: my-travel
  datasource:
    url: jdbc:h2:file:./data/travel-db
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true
